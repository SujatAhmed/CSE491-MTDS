// #include "../include/triangleEnumeration.h"
// #include <iostream>
// #include <ostream>
// #include <vector>
// using namespace std;
//
// float normalize(float x, float k = 0.1) { return x / (x + k); }
// float triangleDensity(int n, vector<vector<int>> adj, int size) {
//   int triangleCount = forward_triangle_listing(n, adj);
//
//   float node_num = size;
//
//   float density = triangleCount / node_num;
//   std::cout << "node num: " << node_num << std::endl;
//
//   // float normalized_density = normalize(density);
//   return density;
// }
