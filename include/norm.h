//norm.h
#ifndef NORM
#define NORM
#include <vector>
#include <map>

using  namespace std;

float norm(
        int triDense,
        int num_nodes,
        float k
);

float hOFx (int k, int x, int M);


#endif // NORM